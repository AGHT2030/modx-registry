/**
 * Stripe → Chain Settlement Adapter
 */

async function settleToChain({ amount, contractType }) {
    // map SKU → contract
    // call on-chain settlement
}

module.exports = { settleToChain };
